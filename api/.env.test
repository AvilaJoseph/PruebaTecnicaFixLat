# Variables de los tests de la API ejecutados en el host (las carga test/setup-env.ts).
# Apuntan a la base de datos de docker compose publicada en localhost:5432. Solo valores de demo.
DATABASE_URL=postgres://app:app@localhost:5432/portal
JWT_SECRET=test-secret-change-me
